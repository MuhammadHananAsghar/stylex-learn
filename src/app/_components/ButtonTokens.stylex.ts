import * as stylex from '@stylexjs/stylex';

export const buttonTokens = stylex.defineVars({
  accent: 'blue',
  background: 'white',
  line: 'gray',
  textPrimary: 'black',
  textSecondary: '#333',
});